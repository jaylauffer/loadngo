[package]
name = "loadngo-renderer"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true

[dependencies]
loadngo-host-core = { path = "../host-core" }
serde = { version = "1", features = ["derive"] }
ui-core = { path = "../ui-core" }
