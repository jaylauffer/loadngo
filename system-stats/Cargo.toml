[package]
name = "loadngo-system-stats"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Allocation-free sampling of CPU, memory, disk, clock, fan and power state for monitors and budgets"

[target.'cfg(target_os = "linux")'.dependencies]
libc = "0.2"
