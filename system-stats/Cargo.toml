[package]
name = "loadngo-system-stats"
version = "0.1.0"
edition.workspace = true
authors.workspace = true
license.workspace = true
description = "Allocation-free sampling of CPU, GPU, power, memory, disk, clock, fan and supply state for monitors and budgets"

[target.'cfg(any(target_os = "linux", target_os = "macos"))'.dependencies]
libc = "0.2"
